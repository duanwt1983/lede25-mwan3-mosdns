# nginx-ubus-module segfaults when POST body exceeds client_body_buffer_size
# (default 8k): the module dereferences a null buffer. LuCI /ubus calls can exceed
# 8k on overview, topology, and config save — raise the in-memory body buffer.
client_body_buffer_size 128k;
