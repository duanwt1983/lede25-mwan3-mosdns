#!/bin/sh /etc/rc.common

START=39
STOP=90

start() {
	# Fallback if uci-defaults did not run; the setup script retires after one use.
	[ -f /etc/.lede-data-init.done ] && return 0
	[ -x /usr/libexec/lede-data-setup ] || return 0
	/usr/libexec/lede-data-setup
}

stop() {
	return 0
}
